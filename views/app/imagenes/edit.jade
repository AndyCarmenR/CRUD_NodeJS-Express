extends ../../layout.jade
block contenido
    div(class="col-md-6 remove-float mx-auto big-top-margin")
        h1(class="text-center") Modificar Imagen
        form(action="/app/imagenes/#{imagen.id}?_method=PUT", method="POST")            
            
            div(class="form-group")
                label(for="titulo") Titulo
                input(type="text", name="titulo", placeholder="Título de la imagen", id="titulo", class="form-control", value="#{imagen.titulo}")
            
            div(class="row top-space")
                div(class="col-xs-12 col-sm-6")        
                    input(type="submit", value="Guardar", class="btn btn-info")